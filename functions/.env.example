# =============================================================================
# BACKEND (Cloud Functions) — parâmetros NÃO-secretos.
# -----------------------------------------------------------------------------
# O SEGREDO (GROQ_API_KEY) NÃO vai aqui — ele é configurado como Secret do
# Firebase (ver GUIA-DEPLOY.md). Este arquivo é só para parâmetros ajustáveis.
# =============================================================================

AI_PROVIDER=groq
GROQ_MODEL=openai/gpt-oss-120b
FREE_DAILY_AI_LIMIT=10
PREMIUM_DAILY_AI_LIMIT=200
